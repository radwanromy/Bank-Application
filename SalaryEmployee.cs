using System;
using System.Collections.Generic;
using System.Text;

namespace BankApp
{
    class SalaryEmployee
    { /*
        private int joinDate;
        public int JoinDate
        {
            get { return joinDate; }
            set { joinDate = value; }
        }
        
        private int lastPaymentDate;

        public int LastPaymentDate
        {
            get { return lastPaymentDate; }
            set { lastPaymentDate = value; }
        }
        
        public SalaryEmployee() { }
        public SalaryEmployee(string name, string id,int balance, int joinDate, int lastPaymentDate) : base(name, id,balance)
        {
            this.joinDate = joinDate;
            this.lastPaymentDate = lastPaymentDate;
            this.balancee = balancee;
            balancee = 0;
        }
        virtual public void EmployeeSalaryAdd(double amount)
        {
            int currentDate = DateTime.Now.Month;
            if (currentDate - lastPaymentDate >= 1)
            {
                balance += amount;
            }
            else
                Console.WriteLine("Account is not matured enough");

        }
        public override void ShowInfo()
        {
            base.ShowInfo();
            Console.WriteLine("Create Year: " + joinDate);
            Console.WriteLine("Tenure Year: " + lastPaymentDate);
            Console.WriteLine("Current Year: " + DateTime.Now.Month);

        } */
    }
}
